# Solo per sviluppo e test: non finisce nell'eseguibile distribuito.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
pytest==9.0.3
# Richiesto da starlette.testclient (FastAPI TestClient) nelle versioni
# recenti di starlette. Nel venv locale c'era gia' da un'installazione
# precedente, quindi il gap si e' visto solo su un ambiente pulito (CI).
httpx2
