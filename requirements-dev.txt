# Development and test only: these packages are not included in the distributed executable.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
pytest==9.0.3
# Required by starlette.testclient (FastAPI TestClient). httpx2 is the
# official successor maintained by the same project; keep it explicitly pinned.
httpx2==2.12.0
