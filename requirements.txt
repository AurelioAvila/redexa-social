fastapi==0.141.1
uvicorn==0.30.6
requests==2.33.0
python-dotenv==1.2.2
google-auth==2.34.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.146.0
# The app's native window (WebView2 on Windows). This was missing from the
# list: it was already installed locally so nobody noticed, but CI starts from
# a clean environment, so PyInstaller was building packages without the module
# desktop_app.py imports on its first working line. The result was an
# executable that opened only to say "No module named 'webview'".
pywebview==6.2.1

# Verify Ed25519 signatures on update manifests. The application only verifies;
# signing remains outside the repository and the distributed executable.
cryptography==50.0.0
