[pytest]
testpaths = tests
# I test importano i moduli dell'app (cache, auth, ...) come fa l'app stessa,
# e le fixture condivise da conftest: entrambe le cartelle devono essere
# raggiungibili senza installare il progetto come pacchetto.
pythonpath = . tests
addopts = -q
