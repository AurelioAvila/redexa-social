[pytest]
testpaths = tests
# The tests import the app's modules (cache, auth, ...) the same way the app
# itself does, plus the shared fixtures from conftest: both directories have
# to be reachable without installing the project as a package.
pythonpath = . tests
addopts = -q
